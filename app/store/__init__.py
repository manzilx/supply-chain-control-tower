"""Storemark — site-store GRN capture (durable SQLite, outside the JSON
snapshot loop). See app/store/db.py for the schema and app/store/grn.py's
confirm_grn() for the single state-mutating path.
"""
